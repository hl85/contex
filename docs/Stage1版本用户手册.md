# Contex Stage 1 用户手册

> **版本**: Stage 1 (v0.5 Alpha)  
> **更新日期**: 2026-01-30  
> **阅读时间**: 约 15 分钟

---

## 目录

1. [快速开始（5 分钟上手）](#1-快速开始5-分钟上手) ⭐
2. [系统要求与资源](#2-系统要求与资源)
3. [详细安装指南](#3-详细安装指南)
4. [配置说明](#4-配置说明)
5. [启动与运行](#5-启动与运行)
6. [日常使用](#6-日常使用)
7. [故障排查](#7-故障排查)
8. [项目结构](#8-项目结构)
9. [附录](#9-附录)

---

## 1. 快速开始（5 分钟上手）

如果你熟悉开发环境配置，按照以下步骤 5 分钟内即可运行 Contex。

### 1.1 前置检查清单

在开始前，请确认你已安装：

| 组件 | 版本要求 | 检查命令 | 必需 |
|------|---------|---------|------|
| **Python** | >= 3.9 | `python3 --version` | ✅ |
| **Node.js** | >= 16 | `node --version` | ✅ |
| **Rust** | 最新稳定版 | `rustc --version` | ✅ |
| **Git** | 任意版本 | `git --version` | ✅ |
| **Docker Desktop** | 4.x+ | `docker --version` | 生产环境 |

> **提示**: 如果某项未安装，请跳转到 [3. 详细安装指南](#3-详细安装指南) 查看安装步骤。
>
> **⚠️ 中国大陆用户注意**: 使用 Docker 模式前，请先配置国内镜像源（见 [3.7 节](#37-配置-docker-镜像源中国大陆用户)），否则镜像下载可能失败。

### 1.2 一键启动（复制即用）

打开终端，执行以下命令：

```bash
# 1. 克隆项目（请替换为实际仓库地址）
git clone https://github.com/your-org/contex.git contex
cd contex

# 2. 创建虚拟环境并激活
python3 -m venv .venv
source .venv/bin/activate

# 3. 安装 Sidecar 依赖
pip install -r apps/sidecar/requirements.txt

# 4. 设置 API Key（必须）
export GOOGLE_API_KEY="你的_Gemini_API_Key"

# 5. 安装 Brain 运行时依赖（Mock 模式）
pip install duckduckgo-search google-genai langchain langgraph beautifulsoup4 pandas playwright
playwright install chromium

# 6. 启动 Sidecar（终端窗口 1）
python apps/sidecar/api/main.py
```

看到 `Uvicorn running on http://127.0.0.1:12345` 表示 Sidecar 启动成功。保持此终端窗口运行。

### 1.3 启动桌面应用

**打开新的终端窗口**（保持 Sidecar 终端运行）：

```bash
cd contex/apps/desktop
npm install
npm run tauri dev
```

看到桌面窗口弹出即表示启动成功！

### 1.4 验证运行

1. 查看 Desktop 右上角状态指示器，应为 🟢 **"Connected"**
2. 点击 **Settings** → 选择 **"每日简报"** → 配置关注话题 → **Save**
3. 返回 Dashboard，点击 **"Run Task"**
4. 观察日志输出，看到 `Workflow executed successfully` 即成功

🎉 **恭喜！Contex 已成功运行！**

---

## 2. 系统要求与资源

### 2.1 硬件要求

| 配置项 | 最低要求 | 推荐配置 | 说明 |
|--------|---------|---------|------|
| **CPU** | 4 核 | 8 核+ | Apple Silicon (M1+) 或同等 x86 |
| **内存** | 8 GB | 16 GB+ | Docker 模式需要更多内存 |
| **磁盘空间** | 10 GB | 20 GB+ | 包含依赖和日志 |
| **网络** | 稳定互联网连接 | - | 用于下载依赖和搜索 |

### 2.2 软件要求

#### 操作系统支持

| 操作系统 | 版本要求 | 支持状态 | 备注 |
|----------|---------|---------|------|
| **macOS** | 14+ (Sonoma) | ✅ 完全支持 | Apple Silicon 优先 |
| **Windows** | 11 (WSL2) | ⚠️ 需要 WSL2 | 需启用 WSL2 和 Ubuntu |
| **Linux** | Ubuntu 22.04+ | ✅ 完全支持 | 其他发行版可能需调整 |

#### 必需软件

1. **Python 3.9+**
   - 推荐 3.11 以获得更好性能
   - 必须使用虚拟环境（venv）

2. **Node.js 16+**
   - 推荐使用 `npm` 或 `bun`
   - 用于构建 Tauri 桌面应用

3. **Rust 最新稳定版**
   - 用于编译 Tauri 原生模块
   - 安装命令：`curl --proto '=https' --tlsv1.2 -sSf https://sh.rustup.rs | sh`

4. **Docker Desktop 4.x+**（生产环境必需）
   - 仅 Docker 模式需要
   - Mock 模式开发调试无需安装

### 2.3 资源使用监控

Contex 的资源占用情况：

| 组件 | 空闲内存 | 峰值内存 | CPU 占用 |
|------|---------|---------|---------|
| **Sidecar** | ~50 MB | ~200 MB | < 5% |
| **Desktop** | ~100 MB | ~300 MB | < 10% |
| **Brain (Docker)** | ~400 MB | < 4 GB | < 50% |

---

## 3. 详细安装指南

如果快速开始的步骤遇到问题，或你是首次配置开发环境，请按照以下详细步骤操作。

### 3.1 安装 Python 和虚拟环境

**macOS / Linux:**

```bash
# 检查 Python 版本
python3 --version  # 应显示 3.9 或更高

# 创建虚拟环境（在项目根目录执行）
cd contex
python3 -m venv .venv

# 激活虚拟环境
source .venv/bin/activate

# 验证激活成功（提示符前会出现 (.venv)）
which python  # 应显示项目目录下的 .venv/bin/python
```

**Windows (WSL2):**

```powershell
# 在 WSL2 Ubuntu 中执行
python3 -m venv .venv
source .venv/bin/activate
```

> **⚠️ 重要**: 所有 Python 相关操作必须在虚拟环境激活状态下执行。

### 3.2 安装 Sidecar 依赖

Sidecar 是 Contex 的核心服务，负责配置管理和任务调度。

```bash
# 确保在项目根目录且虚拟环境已激活
pip install -r apps/sidecar/requirements.txt
```

**依赖清单**:

| 包名 | 版本 | 用途 |
|------|------|------|
| fastapi | 0.109.0 | Web API 框架 |
| uvicorn | 0.27.0 | ASGI 服务器 |
| docker | 7.0.0 | Docker API 客户端 |
| python-dotenv | 1.0.0 | 环境变量管理 |
| pytest | 8.0.0 | 测试框架 |

### 3.3 安装 Brain 运行时依赖（Mock 模式）

Stage 1 默认使用 **Mock 模式**（直接在宿主机运行脚本），需要安装以下依赖：

```bash
# 分步安装（推荐，便于排查问题）

# 1. 搜索和数据处理
pip install duckduckgo-search beautifulsoup4 pandas

# 2. AI 和 Agent 框架
pip install google-genai langchain langgraph

# 3. 浏览器自动化（用于爬虫）
pip install playwright
playwright install chromium
```

**依赖说明**:

| 包名 | 用途 | 必需 |
|------|------|------|
| duckduckgo-search | 新闻搜索 | ✅ |
| google-genai | Gemini AI API | ✅ |
| langgraph | Agent 工作流编排 | ✅ |
| langchain | LLM 抽象层 | ✅ |
| beautifulsoup4 | HTML 解析 | ✅ |
| pandas | 数据处理 | ✅ |
| playwright | 浏览器自动化 | ✅ |

### 3.4 安装 Desktop 应用依赖

Desktop 应用基于 Tauri + React 构建。

```bash
cd apps/desktop

# 使用 npm
npm install

# 或使用 bun（更快）
bun install
```

**依赖说明**:

| 类别 | 主要依赖 |
|------|---------|
| **框架** | React 19, React Router 7 |
| **桌面** | Tauri 2.x, Tauri API |
| **表单** | RJSF (React JSON Schema Form) |
| **构建** | Vite, TypeScript 5.9 |

### 3.5 安装 Rust（如未安装）

Tauri 需要 Rust 工具链：

```bash
# 使用 rustup 安装（官方推荐）
curl --proto '=https' --tlsv1.2 -sSf https://sh.rustup.rs | sh

# 安装完成后，重启终端或执行
source $HOME/.cargo/env

# 验证安装
rustc --version  # 应显示 1.70+
```

### 3.6 安装 Docker Desktop（可选，生产环境）

如需使用 Docker 模式：

**macOS:**
- 下载 [Docker Desktop for Mac](https://www.docker.com/products/docker-desktop)
- 或使用 Homebrew: `brew install --cask docker`

**Windows:**
- 在 WSL2 中安装 Docker Desktop
- 启用 WSL2 集成

**Linux:**
```bash
# Ubuntu/Debian
curl -fsSL https://get.docker.com | sh
sudo usermod -aG docker $USER
```

### 3.7 配置 Docker 镜像源（中国大陆用户）

**⚠️ 注意事项**: 如果你在中国大陆，直接访问 Docker Hub 可能会失败或极慢。**建议在安装 Docker Desktop 后立即配置国内镜像源**。

#### 操作步骤

1. 打开 Docker Desktop
2. 点击右上角 **Settings**（齿轮图标）
3. 选择左侧 **Docker Engine**
4. 在 JSON 配置中添加镜像源：

```json
{
  "builder": {
    "gc": {
      "defaultKeepStorage": "20GB",
      "enabled": true
    }
  },
  "experimental": false,
  "registry-mirrors": [
    "https://mirror.baidubce.com",
    "https://docker.m.daocloud.io",
    "https://docker.1panel.live"
  ]
}
```

5. 点击 **Apply & Restart** 应用并重启

#### 验证镜像源配置

```bash
# 查看当前配置的镜像源
docker info | grep -A 10 "Registry Mirrors"

# 应该输出类似内容：
# Registry Mirrors:
#  https://mirror.baidubce.com/
#  https://docker.m.daocloud.io/
#  https://docker.1panel.live/
```

#### 推荐的国内镜像源

| 镜像源 | 地址 | 状态 | 备注 |
|--------|------|------|------|
| **百度** | `https://mirror.baidubce.com` | ✅ 稳定 | 推荐首选 |
| **DaoCloud** | `https://docker.m.daocloud.io` | ✅ 稳定 | 企业级 |
| **1Panel** | `https://docker.1panel.live` | ✅ 稳定 | 开源社区 |

> **💡 提示**: 可以同时配置多个镜像源，Docker 会自动选择可用的源。

---

## 4. 配置说明

### 4.1 配置优先级（重要！）

Contex 使用三层配置机制，优先级从高到低：

```
┌─────────────────────────────────────────────────────────┐
│  L1: .env 文件（最高优先级，敏感信息）                   │
│     └── GOOGLE_API_KEY, USE_MOCK_DOCKER                 │
├─────────────────────────────────────────────────────────┤
│  L2: config.json（应用配置）                            │
│     └── DEBUG_MODE, LOG_LEVEL, skill_configs            │
├─────────────────────────────────────────────────────────┤
│  L3: 系统环境变量（最低优先级）                          │
│     └── 非敏感配置项                                     │
└─────────────────────────────────────────────────────────┘
```

**关键原则**:

- ✅ **敏感信息**（API Key、密码）→ 放入 `.env` 文件
- ✅ **应用配置**（调试模式、技能配置）→ 放入 `config.json`
- ❌ **永远不要**将敏感信息提交到 Git

### 4.2 推荐配置方案

#### 方案 A：环境变量（推荐用于开发）

在项目根目录创建 `.env` 文件：

```bash
# 编辑 .env 文件
GOOGLE_API_KEY=你的_Gemini_API_Key
USE_MOCK_DOCKER=true
LOG_LEVEL=INFO
```

**优点**:
- 不会被意外提交（已在 `.gitignore` 中）
- 符合安全最佳实践
- 便于切换不同环境

#### 方案 B：config.json（应用配置）

```json
{
  "DEBUG_MODE": true,
  "LOG_LEVEL": "INFO",
  "skill_configs": {
    "daily-brief": {
      "topics": ["AI", "SpaceX", "Technology"],
      "max_results": 5
    }
  }
}
```

**注意**: `config.json` 不应包含敏感键（如 `GOOGLE_API_KEY`），这些会自动被过滤。

### 4.3 获取 Google Gemini API Key

1. 访问 [Google AI Studio](https://makersuite.google.com/app/apikey)
2. 点击 **"Create API Key"**
3. 复制生成的密钥
4. 粘贴到 `.env` 文件: `GOOGLE_API_KEY=你的密钥`

> **⚠️ 安全提醒**: API Key 相当于你的密码，不要分享给他人，不要提交到代码仓库。

### 4.4 运行模式配置

#### Mock 模式（默认，开发调试）

```bash
# .env
USE_MOCK_DOCKER=true
```

**特点**:
- Sidecar 直接在宿主机 Python 环境中运行脚本
- 启动速度快（秒级）
- 便于调试（可直接修改代码）
- **缺点**: 无环境隔离，依赖宿主机环境

#### Docker 模式（生产环境）

```bash
# .env
USE_MOCK_DOCKER=false
```

**前置步骤**:

```bash
# 1. 构建 Brain 镜像（只需执行一次）
docker build -t contex-brain:latest -f packages/brain/Dockerfile .

# 2. 启动 Docker Desktop
open -a Docker  # macOS
```

**特点**:
- 完全环境隔离
- 一致性高（与生产环境一致）
- **缺点**: 首次构建镜像较慢，需要更多资源

---

## 5. 启动与运行

### 5.1 启动流程概览

```
┌──────────────┐      ┌──────────────┐      ┌──────────────┐
│  Terminal 1  │      │  Terminal 2  │      │   Desktop    │
│   (Sidecar)  │  →   │    (Tauri)   │  →   │     App      │
└──────────────┘      └──────────────┘      └──────────────┘
   127.0.0.1:12345       npm run tauri dev      🟢 Connected
```

### 5.2 步骤 1：启动 Sidecar

**打开终端窗口 1**:

```bash
# 进入项目根目录
cd /path/to/contex

# 激活虚拟环境
source .venv/bin/activate

# 启动 Sidecar 服务
python apps/sidecar/api/main.py
```

**成功标志**:

```
INFO:sidecar.config:Loaded application config from /path/to/contex/config.json
INFO:sidecar.api:Sidecar API starting...
INFO:uvicorn:Uvicorn running on http://127.0.0.1:12345
```

**保持此终端窗口运行**，不要关闭。

### 5.3 步骤 2：启动 Desktop App

**打开新的终端窗口 2**:

```bash
# 进入 Desktop 目录
cd /path/to/contex/apps/desktop

# 启动 Tauri 开发模式
npm run tauri dev
```

**首次启动**需要编译 Rust 代码，可能需要 2-5 分钟。

**成功标志**:

- 桌面应用窗口弹出
- 显示 **"Contex Stage 1"** 仪表盘
- 右上角显示 🟢 **"Sidecar Connected"**

### 5.4 验证连接状态

1. **查看 Sidecar 健康状态**:
   ```bash
   curl http://127.0.0.1:12345/health
   # 应返回: {"status": "ok", "component": "sidecar"}
   ```

2. **查看 Desktop 状态指示器**:
   - 🟢 绿色: Connected（连接正常）
   - 🔴 红色: Disconnected（检查 Sidecar 是否运行）

3. **查看日志**:
   ```bash
   # 实时查看日志
   tail -f logs/system.log
   ```

---

## 6. 日常使用

### 6.1 配置技能（Skill）

以 **"每日简报"** 为例：

1. 点击 Dashboard 右上角 **"Settings"** 按钮
2. 在左侧列表选择 **"每日简报 (daily-brief)"**
3. 在右侧表单中配置：
   - **关注话题**: 输入感兴趣的关键词（如 `"DeepSeek", "SpaceX", "Quantum"`）
   - **简报语言**: 选择 "中文" 或 "English"
   - **抓取数量**: 默认 3-5 条
4. 点击 **"Save Configuration"**
5. 确认出现 "Saved successfully!" 提示

### 6.2 运行任务

1. 点击左上角 **"Back to Dashboard"** 返回仪表盘
2. 在 **"Daily Brief Agent"** 卡片中，点击 **"Run Task"** 按钮
3. **观察日志控制台**（Log Console）：
   - `[sidecar.api] Received request to run task: daily-brief`
   - `[skill.daily-brief] === Starting Daily Brief Agent ===`
   - `[skill.daily-brief] Searching news for topics...`
   - `[skill.daily-brief] Workflow executed successfully.`

### 6.3 查看结果

**Stage 1 版本**结果通过以下方式查看：

1. **实时日志**: Desktop 界面中的 Log Console
2. **日志文件**: `logs/system.log`（JSON 格式）
3. **终端输出**: Sidecar 和 Skill 的实时输出

**后续版本**将支持：
- UI 中直接展示 Markdown 简报
- 结果导出功能
- 微信推送

### 6.4 停止服务

**正确关闭顺序**:

1. **关闭 Desktop**: 点击窗口关闭按钮或 `Cmd+Q` (macOS)
2. **关闭 Sidecar**: 在 Terminal 1 中按 `Ctrl+C`
3. **退出虚拟环境**: `deactivate`（可选）

---

## 7. 故障排查

### 7.1 常见问题速查表

| 现象 | 可能原因 | 解决方案 |
|------|---------|---------|
| **🔴 "Disconnected"** | Sidecar 未启动 | 检查 Terminal 1 是否运行 |
| **点击 Run Task 无反应** | 依赖未安装 | 安装 Brain 依赖（见 3.3） |
| **提示 "Configuration Missing"** | 缺少 API Key | 检查 `.env` 文件中的 `GOOGLE_API_KEY` |
| **搜索结果为空** | 网络受限或关键词问题 | 更换关键词，检查网络 |
| **端口冲突** | 12345 被占用 | 关闭占用进程或修改端口 |
| **Docker 模式失败** | 镜像未构建 | 执行 `docker build` 命令 |

### 7.2 详细故障诊断

#### 问题 1: Desktop 显示 "Disconnected"

**诊断步骤**:

1. 检查 Sidecar 是否运行：
   ```bash
   curl http://127.0.0.1:12345/health
   ```
   - 如果无响应 → Sidecar 未启动
   - 如果返回 JSON → Sidecar 正常，检查 Desktop 网络

2. 检查端口占用：
   ```bash
   # macOS
   lsof -i :12345
   
   # Linux
   netstat -tulpn | grep 12345
   ```

**解决方案**:
- 重新启动 Sidecar
- 关闭占用 12345 端口的其他程序

#### 问题 2: 点击 Run Task 后无反应

**检查清单**:

1. **检查 Sidecar 终端是否有报错**
2. **检查 Python 依赖是否完整**:
   ```bash
   source .venv/bin/activate
   python -c "import langgraph; import duckduckgo_search; print('OK')"
   ```
   如果报错 → 重新安装依赖

3. **检查日志**:
   ```bash
   cat logs/system.log | grep ERROR
   ```

#### 问题 3: 提示 "Configuration Missing"

**原因**: 缺少必需的配置项（如 `GOOGLE_API_KEY`）

**解决方案**:

1. 检查 `.env` 文件是否存在：
   ```bash
   ls -la .env
   ```

2. 检查 API Key 是否设置：
   ```bash
   cat .env | grep GOOGLE_API_KEY
   ```

3. 重新加载配置（重启 Sidecar）:
   ```bash
   # Terminal 1: Ctrl+C 停止，然后重新启动
   python apps/sidecar/api/main.py
   ```

#### 问题 4: 搜索结果为空

**可能原因**:
- DuckDuckGo 访问受限（某些网络环境）
- 关键词无相关新闻
- 爬虫被反爬

**解决方案**:
- 尝试更换关键词（如 "Apple", "Tesla"）
- 检查网络连接
- 查看详细日志：
  ```bash
  tail -f logs/system.log | grep skill.daily-brief
  ```

#### 问题 5: Docker 模式启动失败

**检查清单**:

1. Docker Desktop 是否运行？
   ```bash
   docker ps
   ```

2. Brain 镜像是否构建？
   ```bash
   docker images | grep contex-brain
   ```
   如果没有 → 重新构建：
   ```bash
   docker build -t contex-brain:latest -f packages/brain/Dockerfile .
   ```

3. **镜像下载失败？**（常见于中国大陆）
   
   **现象**: 构建时卡在 `pull` 阶段，或报错 `Error response from daemon`
   
   **解决方案**: 配置国内镜像源（见 [3.7 配置 Docker 镜像源](#37-配置-docker-镜像源中国大陆用户)）
   
   **快速验证**:
   ```bash
   # 测试是否能拉取基础镜像
   docker pull python:3.11-slim
   
   # 如果很慢或失败，说明需要配置镜像源
   ```

4. 检查 Docker 日志：
   ```bash
   docker logs <container_id>
   ```

### 7.3 调试技巧

#### 查看详细日志

```bash
# 实时跟踪日志
tail -f logs/system.log

# 过滤特定组件的日志
cat logs/system.log | grep sidecar.api
cat logs/system.log | grep skill.daily-brief

# 查看错误日志
cat logs/system.log | grep ERROR
```

#### 测试 Sidecar API

```bash
# 健康检查
curl http://127.0.0.1:12345/health

# 查看配置
curl http://127.0.0.1:12345/config

# 查看技能列表
curl http://127.0.0.1:12345/skills

# 查看日志
curl http://127.0.0.1:12345/logs
```

#### 验证 MVP 功能

项目包含自动化验证脚本：

```bash
# 完整 MVP 验证
python scripts/verify_mvp_full.py

# 仅测试配置管理
python scripts/test_config_management.py

# 仅测试 Sidecar
python scripts/test_sidecar.py
```

### 7.4 获取帮助

如果以上方法无法解决问题：

1. **收集诊断信息**:
   ```bash
   # 系统信息
   python --version
   node --version
   docker --version
   
   # 日志
   cat logs/system.log
   ```

2. **提交 Issue**: 附上系统信息和日志

---

## 8. 项目结构

了解项目结构有助于排查问题和二次开发。

```
contex/
├── 📁 apps/
│   ├── 📁 desktop/           # Tauri 桌面应用（前端 UI）
│   │   ├── src/
│   │   ├── src-tauri/       # Rust 后端代码
│   │   └── package.json
│   ├── 📁 sidecar/          # Python 核心服务（桥接层）
│   │   ├── api/
│   │   │   └── main.py      # FastAPI 服务入口
│   │   ├── core/
│   │   │   ├── config.py    # 配置管理
│   │   │   ├── docker_client.py  # Docker 客户端
│   │   │   └── logger.py    # 日志系统
│   │   └── requirements.txt
│   └── 📁 gateway/          # 微信/远程网关（可选）
│
├── 📁 packages/
│   ├── 📁 brain/            # Docker 镜像定义
│   │   ├── Dockerfile
│   │   └── requirements.txt
│   └── 📁 skills/           # AI 技能库
│       └── daily-brief/     # 每日简报技能
│           ├── manifest.json
│           └── main.py
│
├── 📁 scripts/              # 自动化脚本
│   ├── verify_mvp_full.py   # MVP 验证脚本
│   └── build_brain.sh       # 镜像构建脚本
│
├── 📁 logs/                 # 日志目录
│   └── system.log           # 系统日志（JSON 格式）
│
├── 📄 .env                  # 环境变量（敏感配置）
├── 📄 config.json           # 应用配置（非敏感）
├── 📄 README.md             # 项目简介
└── 📄 LICENSE
```

### 8.1 关键文件说明

| 文件/目录 | 用途 | 修改建议 |
|----------|------|---------|
| `.env` | 敏感配置（API Key） | ✅ 按环境定制 |
| `config.json` | 应用配置 | ✅ 可以修改 |
| `logs/system.log` | 运行日志 | ⚠️ 自动轮转，无需手动删除 |
| `apps/sidecar/api/main.py` | Sidecar 入口 | ⚠️ 除非你清楚做什么 |
| `packages/skills/` | 技能代码 | ✅ 可以添加新技能 |

---

## 9. 附录

### 9.1 环境变量参考

| 变量名 | 说明 | 示例值 | 必需 |
|--------|------|--------|------|
| `GOOGLE_API_KEY` | Gemini API 密钥 | `AIzaSy...` | ✅ |
| `USE_MOCK_DOCKER` | 运行模式 | `true` / `false` | - |
| `LOG_LEVEL` | 日志级别 | `DEBUG` / `INFO` / `WARNING` | - |
| `PYTHONPATH` | Python 路径 | `.` | - |

### 9.2 常用命令速查

```bash
# 启动服务
python apps/sidecar/api/main.py           # Sidecar
cd apps/desktop && npm run tauri dev      # Desktop

# 配置管理
curl http://127.0.0.1:12345/config        # 查看配置
curl http://127.0.0.1:12345/skills        # 查看技能

# 日志管理
tail -f logs/system.log                   # 实时日志
curl http://127.0.0.1:12345/logs          # API 获取日志
curl -X DELETE http://127.0.0.1:12345/logs # 清空日志

# Docker 操作
docker build -t contex-brain:latest -f packages/brain/Dockerfile .  # 构建镜像
docker images | grep contex-brain         # 查看镜像
docker ps                                 # 查看运行容器

# 验证测试
python scripts/verify_mvp_full.py         # 完整验证
python scripts/test_config_management.py  # 配置测试
python scripts/test_sidecar.py            # Sidecar 测试
```

### 9.3 配置文件模板

**`.env` 模板**:

```bash
# ==========================================
# Contex 环境变量配置
# ==========================================

# 必需：Google Gemini API Key
# 获取地址：https://makersuite.google.com/app/apikey
GOOGLE_API_KEY=你的_API_Key_这里

# 可选：运行模式
# true = Mock 模式（开发调试，默认）
# false = Docker 模式（生产环境）
USE_MOCK_DOCKER=true

# 可选：日志级别
# DEBUG, INFO, WARNING, ERROR
LOG_LEVEL=INFO

# 可选：Python 路径
PYTHONPATH=.
```

**`config.json` 模板**:

```json
{
  "DEBUG_MODE": true,
  "LOG_LEVEL": "INFO",
  "skill_configs": {
    "daily-brief": {
      "topics": [
        "Artificial Intelligence",
        "Space Exploration",
        "Quantum Computing"
      ],
      "max_results": 5,
      "language": "zh"
    }
  }
}
```

### 9.4 更新日志

**Stage 1 (v0.5 Alpha) - 2026-01-30**

- ✅ 本地闭环：支持每日简报生成
- ✅ 配置管理：可视化配置界面
- ✅ 日志监控：实时日志查看
- ✅ Mock/Docker 双模式支持
- ⏳ 远程控制：微信接入（v1.0 规划）
- ⏳ 技能商店：更多技能（后续版本）

---

## 10. 总结与后续

恭喜！你已经完成了 Contex Stage 1 的安装和配置。

### 你可以做什么

1. **每日简报**: 自动抓取和总结新闻
2. **本地数据处理**: 分析桌面文件（开发中）
3. **自定义技能**: 基于模板开发新技能

### 后续版本规划

| 版本 | 核心功能 | 预计时间 |
|------|---------|---------|
| **v1.0 (MVP)** | 微信远程控制、Windows 支持 | Month 2 |
| **v1.5** | 技能商店、第三方 MCP 集成 | Month 4 |
| **v2.0** | 多 Agent 协作、知识库 | TBD |

### 反馈与贡献

- 🐛 **Bug 报告**: 提交 Issue 并附上日志
- 💡 **功能建议**: 欢迎提出新想法
- 🔧 **代码贡献**: 遵循现有代码风格

---

**Happy Hacking with Contex!** 🚀
